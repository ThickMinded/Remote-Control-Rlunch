services:
  - type: web
    name: remote-control-relay
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python relay.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
